## 1. 工件与基线对齐

- [ ] 1.1 建立能力到文档映射表，确认 `proposal`、`design`、`specs` 与现有产品设计文档一一对应
- [ ] 1.2 补充界面设计稿治理信息（存放路径、版本标识、页面映射、变更同步规则）
- [ ] 1.3 明确第一版非目标清单并在评审记录中冻结范围
- [ ] 1.4 确认并冻结第一版默认量化阈值（首屏 3 秒、反馈 1 秒、上传 10MB、对比度 4.5:1）

## 2. 家庭空间与成员体系

- [ ] 2.1 实现家庭空间数据隔离策略并验证跨家庭访问拒绝
- [ ] 2.2 实现邀请制成员加入流程并支持自由角色名称
- [ ] 2.3 为成员管理关键操作补充审计事件采集

## 3. 提议确认到自动草稿闭环

- [ ] 3.1 实现提议创建必填校验与待确认初始状态
- [ ] 3.2 实现单次确认即通过规则并处理重复确认提示
- [ ] 3.3 实现“提议通过 + 自动草稿创建”事务并验证幂等唯一性

## 4. 生活记录生命周期

- [ ] 4.1 实现标题与日期最简创建能力，支持可选感想与图片
- [ ] 4.2 实现草稿与已完成状态流转及关联提议追溯展示
- [ ] 4.3 实现上传失败降级保存与补传入口

## 5. 时间线与筛选体验

- [ ] 5.1 实现按时间倒序时间线与草稿状态显著标识
- [ ] 5.2 实现成员、主题、时间范围三维筛选能力
- [ ] 5.3 实现筛选空状态的引导文案与快捷入口

## 6. 前端皮肤主题

- [ ] 6.1 实现浅色、深色、系统跟随三种主题模式
- [ ] 6.2 实现主题切换入口与用户选择持久化
- [ ] 6.3 校验主题在核心页面与组件中的一致性
- [ ] 6.4 实现主题偏好跨端同步（账号维度存储与读取）

## 7. 访问控制与审计追踪

- [ ] 7.1 定义提议、记录、成员管理的操作权限边界并落地校验
- [ ] 7.2 为关键写操作统一审计字段（操作者、对象、动作、时间、结果、追踪编号）
- [ ] 7.3 建立权限失败与关键异常的检索视图，支持排障追踪

## 8. 媒体治理与数据清理

- [ ] 8.1 定义并实现上传格式与大小约束规则
- [ ] 8.2 落地媒体资源关联策略，防止脏数据与孤儿资源
- [ ] 8.3 建立失效媒体清理任务与失败重试策略

## 9. 可用性与可访问性基线

- [ ] 9.1 为核心页面定义首屏与关键操作反馈时延指标并建立验收脚本
- [ ] 9.2 校验主题对比度与错误提示可读性，覆盖浅色与深色场景
- [ ] 9.3 统一空状态、异常状态交互规范并补充验收用例

## 10. 测试与发布验收

- [ ] 10.1 建立“提议 -> 确认 -> 自动草稿 -> 完善发布”端到端回归用例
- [ ] 10.2 建立时间线筛选、上传降级、重复确认、权限拒绝等异常回归用例
- [ ] 10.3 输出第一版发布验收清单并完成跨角色评审签署

## 11. 前后端规范遵循与一致性校验

- [ ] 11.1 前端实现建立“设计稿页面 -> 代码页面”映射清单，并在评审中逐页核对
- [ ] 11.2 前端样式与主题实现强制对齐主题令牌文件（如 `token.css`）与主题模式规范，禁止绕过令牌直接写硬编码颜色
- [ ] 11.3 前端关键交互文案、状态样式、空态与错误态遵循设计稿与规格双重约束（视觉遵循设计稿，行为遵循 specs）
- [ ] 11.4 后端接口实现统一遵循字段命名、错误码、审计字段与幂等策略规范，并形成接口约束清单
- [ ] 11.5 建立前后端联调检查项：接口契约、主题切换状态、权限边界、异常恢复路径必须全部通过
- [ ] 11.6 代码注释与实现说明按 `design.md` 决策编号标注（例如 `D-02`、`D-03`、`D-04`、`D-06`）
- [ ] 11.7 固定设计稿资产规范：统一使用 `design/` 目录与“日期-版本号”命名规则
- [ ] 11.8 联调前必须完成 `quick-reference.md` 全项自检，并在评审记录中附检查结果
